name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: toughlove-web/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: toughlove-web

      - name: Build
        run: npm run build
        working-directory: toughlove-web

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: toughlove-web/dist
          branch: gh-pages
